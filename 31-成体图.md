# 成体图（终态视图）

> **文档定位**：定义摇篮系统的终态结构——发育完成时长成的系统："由什么组成、各部分怎么通信"。这是发育过程（`30-发育图.md`）结束时刻的结构快照；两视图正交，任一单独不完整（论证见 `32-视图对照.md`）。
>
> **上游依据**：生物学机制见 `10-发育机制与发育周期.md`；概念公理见 `20-思想文档.md`。

## 符号约定

| 符号 | 含义 |
|------|------|
| o_t | t 时刻的多模态感觉输入（视觉/听觉/触觉/本体感觉） |
| s_t = T3(o_t) | 感知编码器输出的潜空间状态表示 |
| a_t | t 时刻系统输出的动作 |
| ŝ_{t+1} | 世界模型对下一时刻状态表示的预测 |
| e_{t+1} = d(s_{t+1}, ŝ_{t+1}) | 预测误差（d 为潜空间距离度量） |
| v_t | 价值系统输出的评估信号 |
| θ_X | 模块 X 的参数（权重） |
| D_i | 发育图中的阶段编号（见 30 §1.2） |

---

## 1.1 总体结构

系统为八模块分层结构，具身接口与外部世界闭合为主回路：

```text
┌─────────────────────────────────────────────┐
│ T1 交付层                                    │
│    语言出口（符号序列） / 动作出口（控制指令） │
└───────────────────┬─────────────────────────┘
                    │（仅交互时启用；非必要不解码）
┌───────────────────┴─────────────────────────┐
│ T2 全局工作记忆（共享黑板）                   │
│    各模块中间产物的汇合区；                    │
│    注意机制选择当前广播的内容                  │
└──┬──────┬──────┬───────┬───────┬──────────┘
   │      │      │       │       │
┌──┴───┐┌─┴────┐┌┴─────┐┌┴─────┐┌┴───────────┐
│ T3   ││ T4   ││ T5   ││ T6   ││ T7         │
│感知  ││世界  ││价值  ││记忆  ││执行系统    │
│编码  ││模型  ││系统  ││系统  ││分层规划与  │
│      ││      ││      ││      ││动作生成    │
└──┬───┘└──┬───┘└──────┘└──────┘└──────┬─────┘
   │       │                           │
                               ┐───────┘
┌──┴───────┴───────────────────┴─────────────┐
│ T8 具身接口                                  │
│    传感器阵列 / 效应器阵列 / 本体感觉回路      │
└───────────────────┬─────────────────────────┘
                    │
          ┌─────────┴─────────┐
          │   外部环境（含导师）│
          └───────────────────┘
```

（图中下行通道：T3/T4 的 ┬ 为感知-预测通路；T7 底 ┬ 经右侧折线为动作 a_t 下发通道；T8 顶其余 ┴ 为感觉输入上行。完整数据流见 1.3 主回路。）

## 1.2 模块定义

每个模块按六要素定义：职责、输入、输出、依赖、生物学对应（详见 10）、失败模式。

### T1 交付层

- **职责**：将 T2 广播的内部状态翻译为外部可接收的输出。两个通道：语言出口（离散符号序列）、动作出口（连续控制指令）。
- **输入**：T2 广播的当前状态。 **输出**：符号序列 / 控制指令。
- **依赖**：T3（符号须锚定于感知表示，见发育阶段 D3）。
- **生物学对应**：语言的运动-感知回路（10 §六）。
- **失败模式**：无 T3/T4 支撑的空转翻译——符号与内部表示脱钩（对应"词语无处挂锚"，见 20 §公理二）。
- **设计立场**：交付层是接口而非智能本体；系统的主回路（下节）不含 T1。

### T2 全局工作记忆

- **职责**：各模块中间产物的共享汇合区；注意机制决定此刻哪些内容被广播、哪些被保持、哪些被替换。
- **输入/输出**：与 T3~T7 双向连接。
- **生物学对应**：前额叶工作记忆；功能上对应全局工作空间（global workspace）的广播结构。
- **失败模式**：广播失效——各模块并行运转但无整合。

### T3 感知编码

- **职责**：多模态感觉输入 → 共享潜空间的状态表示 s_t。多模态在同一潜空间对齐（同一事物的看/听/触对应邻近表示）。
- **输入**：o_t（经 T8）。 **输出**：s_t。
- **生物学对应**：皮层感觉区的分层编码与多模态对齐（10 第一、四节）。
- **失败模式**：表示坍塌——编码器对所有输入输出同一表示（JEPA 文献中的经典失效；防坍塌机制属实现层，蓝图仅标记该风险）。

### T4 世界模型

- **职责**：潜空间动力学预测——给定当前状态与动作，预测下一状态表示：ŝ_{t+1} = T4(s_t, a_t)。动作条件化使其成为可用于规划的模型（在潜空间前瞻搜索动作序列）。
- **输入**：s_t、a_t。 **输出**：ŝ_{t+1}。
- **依赖**：T3。
- **生物学对应**：预测加工层级（10 第二节）。
- **失败模式**：只记表面规律（应丢弃的细节未丢弃）或过度泛化（本可预测的结构被丢弃）。

### T5 价值系统

- **职责**：将预测误差的动态转换为评估信号 v_t。内部为**多驱动生态**（非单一奖励）：好奇（新异误差吸引）、不适（持续高误差回避）、胜任（误差下降正反馈）、社交（对导师反应的敏感）。多驱动加权随发育阶段变化（D3 前社交驱动休眠）。
- **输入**：e_t 序列及其变化率、T6 的情境。 **输出**：v_t（对当前状态/候选动作的评估）。
- **生物学对应**：多巴胺系统的奖励预测误差与内在动机（10 第三节）。
- **失败模式**：单驱动独大——如好奇无不适制约则系统无法稳定停留；驱动间必须可相互抑制（对应 20 §2.4"驱动之间必须能打架"）。

### T6 记忆系统

- **职责**：双速结构。快部件：情景缓冲，一次经验即时写入（s_t, a_t, e_t, v_t）；慢部件：参数化长期知识，经离线重放将高频情景固化为 T3/T4/T7 的参数更新。
- **输入**：主回路的经验流。 **输出**：巩固信号（慢速更新各模块参数）；按需向 T2 提取情景。
- **生物学对应**：互补学习系统——海马快写、皮层慢固化、睡眠重放（10 第八节）。
- **失败模式**：只快不慢（经验堆积不固化）；只慢不快（慢系统缺少固化原料）。

### T7 执行系统

- **职责**：分层规划与动作生成。低层：反应式动作选择；高层：利用 T4 在潜空间前瞻模拟多条动作序列，选评估最优者下发。高层规划依赖 D4 建立的反事实能力。
- **输入**：s_t、v_t、目标状态（内部生成的长期倾向）。 **输出**：a_t。
- **依赖**：T4（前瞻模拟的模型）、T5（序列评估）。
- **生物学对应**：前额叶的分层控制与运动系统的层级组织（10 第一、七节）。
- **失败模式**：无 T4 的高层规划退化为无前瞻的逐步反应。

### T8 具身接口

- **职责**：感觉运动通道——传感器阵列采集环境状态，效应器阵列执行动作，本体感觉回路携带"自身动作"信号（区分"世界变了"与"我动了世界"）。
- **输入/输出**：环境 ⇄ 系统。
- **生物学对应**：感觉运动系统与 motor babbling 的动作-感觉绑定（10 第四节）。
- **设计立场**：**"身体"取广义定义——任何能在环境中行动并承受后果的接口**（机械躯体、模拟环境中的虚拟效应器均可）。但必须是**行动有真实后果**的通道：只读不动的观察接口不构成身体。

## 1.3 主回路（核心数据流的形式化）

系统的心跳是以下闭环（伪代码）：

```text
loop t:
    o_t    = T8.sense()                        # 感觉输入
    s_t    = T3(o_t, T8.proprioception())      # 感知编码（含本体感觉）
    e_t    = d(s_t, ŝ_t)                       # 预测误差（对照上一步的预测）
    v_t    = T5(e_t, Δe_t, T6.context())       # 价值评估（误差水平+变化率+多驱动加权）
    a_t    = T7(s_t, v_t, goal_t)              # 动作决策（高层走 T4 前瞻，低层直反应）
    ŝ_t+1  = T4(s_t, a_t)                      # 生成对下一步的预测
    T6.write(s_t, a_t, e_t, v_t)               # 情景写入（快）
    T6.consolidate()                           # 离线重放 → 慢速更新 θ_T3, θ_T4, θ_T7
    T8.act(a_t)                                # 作用于环境
    T2.broadcast(current)                      # 工作记忆广播
```

三条结构性说明：

1. **学习信号全部来自 e_t**：主回路无外部标注、无外部奖励；T5 只消费误差动态，不制造监督信号（公理三的实现）。
2. **T1 不在主回路内**：语言出口只在交互场景被调用——说话是系统的接口行为，不是系统的运行方式。
3. **参数变化只发生在巩固步**：T6.consolidate 是唯一改写 θ 的入口（权重继承的实现载体，见 30 §1.3）。

## 1.4 成体图的盲区（为什么必须补发育视图）

静态视图有三处结构性画不出：

- **课程**：难度递增的来源不在任何模块内，而在"系统能力 × 环境结构"的交互动力学中（20 §2.2 的三因子夹逼）；
- **时序**：模块的**就绪顺序**与**冻结窗口**（敏感期）是时间属性；
- **成长**：同一解剖结构在不同发育阶段呈现不同的主导模块与连接强度。

三者的共同特征：**都是过程属性**。以时间为轴的视图见 `30-发育图.md`；两视图的正交性论证与叠加分析见 `32-视图对照.md`。
