# 成体图（终态视图）

> **文档定位**：定义摇篮系统的终态结构——发育完成时所长成的系统："由什么组成、各部分怎么通信"。这是发育过程（`30-发育图.md`）结束时刻的结构快照；两视图互补，任一单独不完整（论证见 `32-视图对照.md`）。
>
> **上游依据**：生物学机制见 `10-发育机制与发育周期.md`；概念公理见 `20-思想文档.md`。

## 符号约定

| 符号 | 含义 |
|------|------|
| o_t | t 时刻的多模态外感受输入（视觉/听觉/触觉，经 T8 传感器阵列） |
| prop_t = T8.proprioception() | 本体感觉输入（自身动作/姿态信号，与外感受分开传递） |
| s_t = T3(o_t, prop_t) | 感知编码器输出的潜空间状态表示 |
| a_t | t 时刻系统输出的工具性动作（主回路行为） |
| ŝ_{t+1} | 世界模型对下一时刻状态表示的预测 |
| e_t = d(s_t, ŝ_t) | 预测误差：t 时刻实际状态对上一步预测的偏差（d 为潜空间距离度量） |
| v_t | 价值系统输出的即时评估信号 |
| goal_t | 价值系统输出的长期追求状态（多驱动加权合成，供 T7 规划） |
| θ_X | 模块 X 的参数（权重） |
| D_i | 发育图中的阶段编号（见 30 §1.2） |

---

## 1.1 总体结构

系统为八模块分层结构，经具身接口与外部世界闭合成主回路：

```text
┌──────────────────────────────────────────────────┐
│ T1 交付层（交互接口，不在主回路内）              │
│    语言出口（符号序列）/ 动作出口（表达性动作）  │
└───────────────────┬──────────────────────────────┘
                    │ 仅交互时调用；输出经 T8 效应器执行
┌───────────────────┴──────────────────────────────┐
│ T2 全局工作记忆（共享黑板）                      │
│    各模块中间产物的汇合区；注意机制选广播内容    │
└───┬─────────┬─────────┬─────────┬──────────┬─────┘
    │         │         │         │          │
┌───┴────┐┌───┴────┐┌───┴────┐┌───┴────┐┌────┴─────┐
│ T3     ││ T4     ││ T5     ││ T6     ││ T7       │
│感知    ││世界    ││价值    ││记忆    ││执行系统  │
│编码    ││模型    ││系统    ││系统    ││规划与    │
│        ││        ││        ││        ││动作生成  │
└───┬────┘└────────┘└────────┘└────────┘└────┬─────┘
    │ o_t / prop_t 上行                      │ a_t 下发
┌───┴────────────────────────────────────────┴─────┐
│ T8 具身接口                                      │
│    传感器阵列 / 效应器阵列 / 本体感觉回路        │
└───────────────────┬──────────────────────────────┘
                    │ 感觉 ⇄ 行动
          ┌─────────┴─────────┐
          │ 外部环境（含导师）│
          └───────────────────┘
```

（图中竖线仅画两条主干：o_t/prop_t 经 T8→T3 上行、a_t 经 T7→T8 下发；T2 与 T1~T7 之间的双向连线省略为框顶接口标记，完整数据流见 1.3 主回路。T1 的交付输出同样经 T8 效应器执行——说话与表达也是身体的行为。）

## 1.2 模块定义

每个模块按六要素定义：职责、输入、输出、依赖、生物学对应（详见 10）、失败模式。

### T1 交付层

- **职责**：将 T2 广播的内部状态翻译为外部可接收的输出。两个通道：语言出口（离散符号序列）、动作出口（表达性动作——演示、指向等交互场景的动作化表达；工具性动作是 T7 的职责，不经 T1）。交付输出经 T8 效应器执行——说话与表达也是身体的行为。
- **输入**：T2 广播的当前状态。 **输出**：符号序列 / 表达性动作指令（均经 T8 执行）。
- **依赖**：T3 + T4（符号须锚定于世界模型的客体结构——D2 的产物、D3 的绑定，见 20 §公理二）。
- **生物学对应**：语言的感知-运动耦合与共同注意回路（10 §四、§六）。
- **失败模式**：无 T3/T4 支撑的空转翻译——符号与内部表示脱钩（对应"词语无处挂锚"，见 20 §公理二）。
- **设计立场**：交付层是接口而非智能本体；系统的主回路（下节）不含 T1。

### T2 全局工作记忆

- **职责**：各模块中间产物的共享汇合区；注意机制决定此刻哪些内容被广播、哪些被保持、哪些被替换。
- **输入**：T3~T7 写入的中间产物（s_t、e_t、v_t、a_t，及按需的候选动作序列与情景提取结果）。 **输出**：注意机制选中的内容（current）向 T1~T7 广播——T1 据此生成交付输出，T7 据此整合规划上下文。
- **依赖**：T3~T7（汇合它们的产物）、T1（广播的消费方）。
- **生物学对应**：前额叶工作记忆与执行功能（10 §七）；功能上对应全局工作空间（global workspace）理论的广播结构。
- **失败模式**：广播失效——各模块并行运转但无整合。

### T3 感知编码

- **职责**：多模态感觉输入 → 共享潜空间的状态表示 s_t。多模态在同一潜空间对齐（同一事物的看/听/触对应邻近表示）。
- **输入**：o_t（外感受）、prop_t（本体感觉），均经 T8。 **输出**：s_t。
- **依赖**：T8（感觉通道的来源）。
- **生物学对应**：皮层感觉区的分层预测编码与多模态对齐（10 §二、§四）。
- **失败模式**：表示坍塌——编码器对所有输入输出同一表示（JEPA 文献中的经典失效；防坍塌机制属实现层，蓝图仅标记该风险）。

### T4 世界模型

- **职责**：潜空间动力学预测——给定当前状态与动作，预测下一状态表示：ŝ_{t+1} = T4(s_t, a_t)。动作条件化使其成为可用于规划的模型（在潜空间前瞻搜索动作序列）。
- **输入**：s_t、a_t。 **输出**：ŝ_{t+1}。
- **依赖**：T3（被预测的状态表示由它产生）。
- **生物学对应**：预测加工层级（10 §二）。
- **失败模式**：欠抽象（应丢弃的细节未丢弃）或过度泛化（应保留的结构被丢弃）。

### T5 价值系统

- **职责**：将预测误差的动态转换为评估信号。内部为**多驱动生态**（非单一奖励）：好奇（新异误差吸引）、不适（持续高误差回避）、胜任（误差下降正反馈）、社交（对导师/同类反应的敏感）。多驱动加权随发育阶段变化（D3 前社交驱动休眠）——加权的变化经 θ_T5 的慢速更新承载（见 T6 巩固），初始加权由 D0 先验给定。
- **输入**：e_t 及其变化率（均由主回路传入）、T6 供给的 context。 **输出**：v_t（即时评估信号）；goal_t（多驱动加权合成的长期追求状态，供 T7 规划）；T5.evaluate(ŝ 轨迹)——对 T4 生成的候选状态轨迹的序列评估（供 T7 前瞻选优）。
- **依赖**：T4（预测动态的来源）、T6（评估的情境）。
- **生物学对应**：多巴胺系统的奖励预测误差与内在动机（10 §三）。
- **失败模式**：单驱动独大——如好奇无不适制约则系统无法稳定停留；驱动间必须可相互抑制（对应 20 §2.4"驱动之间必须能打架"）。

### T6 记忆系统

- **职责**：双速结构。快部件：情景缓冲，少数几次经验即写入（s_t, a_t, e_t, v_t）；慢部件：参数化长期知识，经离线重放训练——重放内容与既有参数化知识交错整合（不得只灌新经验）、单次更新量极小，逐步固化为 T3/T4/T5/T7 的参数更新（已冻结模块除外，冻结偏序见 30 §1.4）。
- **输入**：主回路的经验流。 **输出**：巩固信号（慢速更新各模块参数）；按需向 T2 提取情景；向 T5 供给 context（最近 k 步经验的摘要，用于价值评估的情境校正）。
- **依赖**：主回路各步（经验流的来源）、T2（按需提取的请求方）。
- **生物学对应**：互补学习系统——海马快写、皮层慢固化、睡眠重放（10 §八）。
- **失败模式**：只快不慢（经验堆积不固化）；只慢不快（慢系统缺少固化原料）。

### T7 执行系统

- **职责**：分层规划与动作生成。低层：反应式动作选择；高层：利用 T4 在潜空间前瞻模拟多条动作序列，以 T5.evaluate 选评估值最优的序列下发。高层规划依赖 D4 建立的反事实能力。
- **输入**：s_t、v_t、goal_t、current（T2 广播的规划上下文，取上一轮广播）。 **输出**：a_t。
- **依赖**：T4（前瞻模拟的模型）、T5（序列评估）。
- **生物学对应**：前额叶的分层控制与运动系统的层级组织（10 §四、§七）。
- **失败模式**：无 T4 的高层规划退化为无前瞻的逐步反应。

### T8 具身接口

- **职责**：感觉运动通道——传感器阵列采集环境状态，效应器阵列执行动作（含 T7 的工具性动作与 T1 的交付输出），本体感觉回路携带"自身动作"信号（区分"自身动作引起的变化"与"外部原因引起的变化"）。
- **输入**：环境的物理信号；T7 的 a_t 与 T1 的交付输出。 **输出**：o_t（外感受）、prop_t（本体感觉）；对环境的作用。
- **依赖**：T7/T1（动作指令来源）、环境（行动的承受者与感觉的来源）。
- **生物学对应**：感觉运动系统与 motor babbling 的动作-感觉绑定（10 §四）。
- **失败模式**：传感器漂移（编码系统性偏移，预测误差出现无来源的偏差）；效应器无后果反馈（动作未被执行而系统以为已执行——"我动了"与"世界变了"的区分失效）。
- **设计立场**：**"身体"取广义定义——任何能在环境中行动并承受后果的接口**（机械躯体、模拟环境中的虚拟效应器均可）。但必须是**行动有真实后果**的通道：只读不动的观察接口不构成身体。

## 1.3 主回路（核心数据流的形式化）

系统的心跳是以下闭环（伪代码）。初始化：ŝ_0 := s_0（首轮无预测，e_0 := 0）：

```text
loop t:
    o_t    = T8.sense()                        # 外感受输入
    s_t    = T3(o_t, T8.proprioception())      # 感知编码（外感受 + 本体感觉）
    e_t    = d(s_t, ŝ_t)                       # 预测误差（对照上一步的预测）
    v_t, goal_t = T5(e_t, Δe_t, T6.context())  # 价值评估（误差水平+变化率+多驱动加权）与长期追求状态
    a_t    = T7(s_t, v_t, goal_t, current)     # 动作决策（高层：T4 前瞻模拟 + T5.evaluate 选优；低层：直接反应；current 为上一轮广播的规划上下文）
    ŝ_{t+1} = T4(s_t, a_t)                     # 生成对下一步的预测
    T6.write(s_t, a_t, e_t, v_t)               # 情景写入（快）
    T8.act(a_t)                                # 作用于环境
    T2.write(s_t, e_t, v_t, a_t)               # 中间产物写入黑板
    T2.broadcast(current)                      # 广播（current = 注意机制从黑板内容选中的广播集）
    on 静息期（低频触发，非每步）:
        T6.consolidate()                       # 离线重放（与既有知识交错）→ 慢速更新 θ_T3, θ_T4, θ_T5, θ_T7（已冻结模块除外）
```

四条结构性说明：

1. **学习信号全部来自 e_t**：主回路无外部标注、无外部奖励；T5 不制造监督信号——输入只有误差动态与情境摘要（公理三的实现）。
2. **T1 不在主回路内**：交付出口只在交互场景被调用——说话是系统的接口行为，不是系统的运行方式；其输出也经 T8 效应器执行。
3. **参数变化只发生在巩固步**：T6.consolidate 是唯一改写 θ 的入口（权重继承的实现载体，见 30 §1.3）；巩固在静息期低频触发（生物对应：睡眠期重放，见 10 §八），不阻塞行动。此条为工程强化：生物原型中慢系统在线亦有微小学习（HM 的内隐学习保存即证），本设计将其收拢至巩固步批量实施；论文约束的是单次更新量极小与交错整合，不约束更新位置。
4. **黑板与直调是同一数据流的两种画法**：1.1 图把 T2 画成汇合中枢，伪代码为可读性写成点对点直调——每步的中间产物同时经 T2.write 汇入黑板（见伪代码 T2.write 行），交互场景与注意机制从黑板取内容（细节属实现层）。

## 1.4 成体图的盲区（为什么必须补发育视图）

静态视图有三处结构性画不出：

- **课程**：难度递增的来源不在任何模块内，而在"系统能力 × 环境结构"的交互动力学中（20 §2.2 的三因子夹逼）；
- **时序**：模块的**就绪顺序**与**冻结窗口**（敏感期）是时间属性；
- **成长**：同一模块结构在不同发育阶段的主导分工与连接强度不同。

三者的共同特征：**都是过程属性**。以时间为轴的视图见 `30-发育图.md`；两视图的互补性论证见 `32-视图对照.md`。
