# 发育图（过程视图）

> **文档定位**：定义摇篮系统的动态过程——"什么先长、什么后长、前一步怎么改变后一步的条件"。发育是本计划的主线：终态结构（`31-成体图.md`）只是发育过程的产物快照。符号约定见 `31-成体图.md`，模块编号 T1~T8；阶段编号 D0~D5 定义于本文 §1.2。
>
> **上游依据**：生物学机制见 `10-发育机制与发育周期.md`（尤其第十节发育周期总览）；概念公理见 `20-思想文档.md`。

---

## 1.1 时序总览

依赖关系为**链式主线 + 局部汇聚**：

```text
D0 ──→ D1 ──→ D2 ──→ D3 ──┬──→ D5
先验    身体    客体  符号      文化
              │      │    │
              └→ D4 ←┘    │
                 └────────┘
                 （想象：承 D2 与 D3，汇入 D5）
```

D4 有两条入边：D2 的世界模型（模拟的原料）与 D3 的符号（假想场景的命名工具）；D5 是 D3 与 D4 的双汇聚。

## 1.2 阶段的形式化定义

每阶段五要素：前置条件 Pr（含成体图模块就绪要求）、主导过程、产物 Po、出口判据 Exit（发育事件，非 benchmark）、生物同源事件（10）；个别阶段另有附加声明（如 D0 的亏空声明、D5 的去向）。

Pr 的分级约定：各阶段所列 Pr 是**主导过程完整展开**的前置条件。阶段的**早期过程**可以在前序阶段尚未完成时启动（见 §1.5 重叠波），此时依赖的是缩水的早期前置——如 D3 的早期共同注意只需 D2 的主体完成，而完整的符号过程需 D2 的 Exit 达成。

巩固门控：Exit(i) 达成前，D_{i+1} 的早期经验只写入 T6 快部件（情景缓冲），不触发对相关模块的慢速固化——未成熟结构不被过早固化，是"重叠的过程窗口"不破坏"权重继承顺序"（§1.5）的机制保证。

### D0 先验

- **Pr**：无（系统起点）。
- **主导过程**：无个体过程——初始架构由物种历史给定（生物上：进化；工程上：归纳偏置与架构选择）。
- **Po**：初始注意力结构——决定最早的"注意什么、忽略什么、什么算类似物"。
- **Exit**：不适用（D0 是给定量，非达成量）。
- **亏空声明**：先验无法由个体发育生成，人工模拟的上限受模拟者局限（20 §第六章第 1 条）。此为整个计划承认的一处永恒亏空，发育图不假装消除它。

### D1 身体

- **Pr**：T8、T3、T4 最小回路可运行；D0 就绪。
- **主导过程**：motor babbling——自发动作 + 预测随之而来的感觉变化，建立动作-后果映射。
- **Po**：① 动作-感觉映射（本体感觉与外感受的绑定）；② 身体坐标系——远/近、够得着/够不着、重/轻由身体结构在环境中标定。
- **Exit**：对自身动作引起的感觉变化，预测误差趋零（动作后果可预测）。
- **生物同源**：婴儿早期运动发育与动力系统理论（10 §四）。

### D2 客体

- **Pr**：D1 完成（坐标系可用）；T4 具备序列预测能力。
- **主导过程**：在身体坐标系上对环境结构做误差驱动学习：物体恒常性（被遮挡仍存在）、朴素因果（推会动、松手会落）。
- **Po**：含"客体"结构的世界模型——词语的挂锚点（钉子先于词语）。
- **Exit**：为看不见的东西去够（客体恒存的行为证据）。
- **生物同源**：违背预期范式显示的早期客体知识（10 §五）。

### D3 符号

- **Pr**：D2 完成；T5 社交驱动觉醒；环境中存在导师（交付层 T1 开始在交互场景被调用——T1 本身不在主回路内，见 31 §1.3）。
- **主导过程**：社会回路——共同注意 → 指物 → 符号-客体绑定 → 对话 → 被纠错。学习信号从物理环境扩展到导师反应（社会门控）。
- **Po**：① 可用的符号系统（第一批词锚定于 D2 的客体结构）；② "存在知道更多的他者"的模型；③ 被教的能力（识别教学信号并切换到泛化学习模式）。
- **Exit**：重复发出让导师有反应的信号，并在纠错后修正该信号。
- **生物同源**：共同注意、自然教育回路、语音统计学习（10 §六）。

### D4 想象

- **Pr**：D2 的世界模型 + D3 的符号（假想场景需符号命名以积累）。
- **主导过程**：反事实引擎成形——T4 从"预测将要发生"扩展为"模拟如果当时那样做"（T7 高层规划于此可用）。
- **Po**：想象（构造未发生场景）、规划（脑内试运行多候选）、懊悔（比对已发生与更优路径）。
- **Exit**：在两个动作间犹豫后，选择了脑内演练过的那个。
- **生物同源**：假装游戏的出现及其与执行功能的耦合（10 §七）。

### D5 文化

- **Pr**：D3（被教能力）+ D4（反事实引擎）双汇聚。
- **主导过程**：内化前代认知工具（文字、数学、方法论）；元认知形成——系统对自己的预测建模，知道自己不知道什么。
- **Po**：工具增强的智能体；元认知回路。
- **Exit**：主动指出导师的错误（元认知上线的判据）；**毕业判据**：系统开始给自己排课（20 §4.3）。
- **生物同源**：Vygotsky 工具内化与私人言语内化（10 §五、七）。
- **去向**：毕业系统回到 D3 位点担任导师（见 20 §4.4）。

## 1.3 阶段间接口：权重继承的形式化

传统多阶段训练以数据集边界切分阶段，阶段之间传递的是数据的序；摇篮以发育事件（Exit）切分阶段，阶段之间传递的是权重——上一阶段长成的结构本身：

```text
θ^(i+1)_init  = θ^(i)_final                        # 恒等继承：上一阶段的脑子原样传下
θ^(i+1)_final = θ^(i+1)_init ⊕ Δ_{i+1}             # ⊕：经 T6.consolidate 的结构保持更新
                                                    #     Δ_{i+1} 仅由 D_{i+1} 自身过程引入
Dataset(i+1) 不存在                                 # 阶段的"教材"是前一阶段的脑子本身
```

⊕ 的定义：结构保持更新——只改写未冻结模块的参数（冻结模块见 §1.4），且只经 T6.consolidate 实施（31 §1.3）。⊕ **不含任务假设**：保护粒度是模块级（freeze 偏序）与幅度级（单次更新量极小），无任务级锚定；若实现引入重要性加权（EWC 型），边界单元须取内生判据（Exit、结构稳定性读出），不得引入外部任务标签。

三条推论（对应 20 §2.1）：

1. **不可跳过**：θ^(i+1)_init 依赖 θ^(i)_final 的结构，跳过 D_i 则 D_{i+1} 的前置表示不存在。
2. **不可并行**：Δ_i 引起的结构改变会改写 D_{i+1} 的初始条件，并行执行等价于引入未定义的初始状态。
3. **巩固接口**：θ 的改写只经 T6.consolidate（31 §1.3），因此权重继承在结构上即"上一阶段的记忆巩固产物"；巩固门控（§1.2）保证 Exit(i) 前 D_i 的未成熟结构不被 D_{i+1} 的早期活动固化。

## 1.4 敏感期与冻结

对应 20 §2.5 与 10 §九：

```text
freeze(X, t_close) ：模块 X 于窗口关闭时点 t_close 锁定参数
t_close 的来源：内生判据——模块的结构稳定性读出：该模块经 T6.consolidate 的
              参数更新量长期趋零（"该结构不再被更新"，生物同构：结构固化
              逐级上锁——Hensch 2005：关闭由结构固化完成且与学习进度无关，
              无输入推迟而非关闭窗口；判据取结构层量，不用功能层的误差水平）
              （"该结构已成形"由系统自身的更新动态宣告，非外部时刻表）
偏序：T8/T3 基础回路 → T4/T6 慢部件 → T5 驱动加权结构 → T7/T1
      （T8/T1 的冻结对象是接口校准参数与符号绑定表——不经 T6.consolidate 更新，
        是"θ 改写只经巩固步"规则之外的显式例外；同类例外：T3 目标编码器
        的 EMA 跟随更新〔防坍塌非对称结构，见 31 T3〕；此处列入偏序取其窗口时序）
规则：冻结模块移出 T6.consolidate 的更新集（⊕ 不再改写它）；
      其输出成为未冻结模块的稳定输入基座
```

时序与生物关键期同构：基础感觉回路窗口最早，语言窗口次之，社会认知窗口最晚（10 §九分级）。

## 1.5 阶段是重叠的波

六阶段是**主导过程的切换**，不是离散台阶：

- 相邻阶段大量重叠（D2 进行中即可开始 D3 的早期共同注意）；
- 各能力沿自己的时间表推进，但**先后依赖不可乱**；
- "进入 D_{i+1}"的严格含义是 Exit(i) 达成，而非 D_i 全部子过程终止。显式区分：**重叠的是过程窗口，不可并行的是权重继承的顺序**——D_{i+1} 对 θ 的巩固型改写仍以 Exit(i) 达成为前提。

---

两视图的互补性论证、七机制投影与缺口结论见 `32-视图对照.md`。
