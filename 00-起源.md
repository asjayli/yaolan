# 起源：现有方向分析

> 记录摇篮计划产生前，对现有 AI 发展方向的梳理（2026-08，与智谱清言 ChatGLM5 的会话）。只做客观分析，不评判各方向优劣——各方向回答的是不同问题，处于不同阶段。

---

## 一、讨论起点：LLM 与 JEPA 的原理差异

**根本差异只有一条：预测目标所处的空间。**

| 维度 | LLM（自回归大语言模型） | JEPA（联合嵌入预测架构） |
|------|------------------------|--------------------------|
| 预测目标 | 离散 token（词/子词）的概率分布 | 输入在潜空间中的连续表示向量 |
| 损失形式 | 最小化交叉熵（即最大化 P(wₙ₊₁\|w₁:ₙ)） | 能量函数/嵌入距离最小化 ‖s_y − ŝ‖ |
| 核心组件 | 单一 Transformer 解码器 | 双编码器（Context + Target）+ 预测器 Predictor |
| 学习范式 | 生成式——显式生成每个细节 | 判别/预测式——只建模"哪些表示是可能的" |
| 不确定性处理 | 通过 softmax 分布覆盖多分支 | 通过潜变量或编码器不变性吸收不可预测细节 |
| 世界模型 | 隐式存在于参数中 | 显式预测未来状态表示，可支持规划 |
| 规划能力 | 逐步生成（System 1 式） | 可在潜空间做分层规划（H-JEPA，System 2 式） |
| 防坍塌机制 | 不需要（生成目标天然有锚） | 需要：EMA 教师编码器、方差/协方差正则等 |

### 1.1 LLM 的结构性特征

- **逐 token 生成**：每个 token 无论信息密度高低都要显式生成；每个 token 都有出错概率，错误随序列长度累积。
- **思考即生成**：Think 模型的"思考"是把草稿写成 token 流（用生成更多 token 模拟思考），推理链本身仍是逐 token 的。COCONUT 等研究已在尝试把推理放入 hidden state 层面，是向潜空间思考靠拢的信号。

### 1.2 JEPA 的实现原理（I-JEPA 为例）

三模块流水线：

- **Context Encoder**：主编码器，输入为整图移除目标块后的上下文区域（约 80%–85% patch），梯度更新——训练完成后保留的最终产物。
- **Target Encoder**：与主编码器同结构，逐个编码被遮挡的目标块（每个目标块单独前向），输出该块的真实表示作为"标准答案"；**不参与梯度，走 EMA 更新**（θ_target = m·θ_target + (1−m)·θ_context，m≈0.996），用于防止两网络合谋输出常量（表示坍塌）。
- **Predictor**：窄 ViT 预测头，从上下文表示 + mask token 预测目标表示；训练完丢弃。
- **Masking 策略**：大块遮挡而非小 patch——目标块按尺度（相对整图 0.85–1.0）与长宽比采样，合计约占图像 15%–20%，迫使模型理解物体结构而非纹理插值。
- **损失**：只有潜空间 L2 距离，**没有像素重建**——编码器因此可以丢弃不可预测细节，只保留语义结构。
- **V-JEPA 2 扩展**：输入改为时空 tubelet，预测器接收动作指令 aₜ 作为输入（sₜ₊₁ = Predictor(sₜ, aₜ)），使潜空间预测成为可操作的世界模型；机器人规划在潜空间完成后，经外挂动作解码器映射为指令。

训练循环伪代码（Java 风格）：

```java
// 初始化
Encoder contextEncoder = new ViT_H14();        // 梯度更新
Encoder targetEncoder = contextEncoder.copy(); // EMA更新，不参与梯度
Predictor predictor = new ViT(depth=6, dim=384); // 轻量预测头
double momentum = 0.996;

for (Batch batch : unlabeledImages) {  // 纯自监督，无需标签
    // 1. 采样遮挡方案
    MaskPlan plan = sampleMultiBlockMask(batch.image);
    // plan.contextPatches: 上下文区域（整图移除目标块后，约 80-85%）
    // plan.targetBlocks:   4 个大尺度目标块（合计约 15-20%）

    // 2. 双路编码
    Embedding[] s_x = contextEncoder.forward(plan.contextPatches);
    Embedding[] s_y = targetEncoder.forward(plan.targetBlocks); // no_grad

    // 3. 预测：预测器拿上下文表示 + mask token，猜目标表示
    Embedding[] pred = predictor.forward(s_x, plan.maskTokens);

    // 4. 损失只在潜空间
    double loss = l2Distance(pred, s_y);

    // 5. 反向传播：只更新 contextEncoder 和 predictor
    optimizer.zeroGradients();
    loss.backward();  // targetEncoder不在计算图里
    optimizer.step(contextEncoder, predictor);

    // 6. EMA更新Target Encoder（像刷新只读副本）
    targetEncoder.emaUpdate(contextEncoder, momentum);
}
// 训练完成后：丢弃targetEncoder和predictor
// 只保留contextEncoder——它就是学好的语义编码器，用于下游任务
```

### 1.3 潜向量：两条路线共享的地基

- 潜向量就是向量——数学上无区别。"潜"字标记**出身**（学习压缩的产物）与**性质**（几何结构承载语义：距离近≈语义相似）。
- LLM 内部本来就是"token → 潜向量 → 计算 × N 层 → 潜向量 → 翻译回 token"的机器：token 只在两端各存在一步，几乎全部计算发生在潜向量空间。
- 因此两条路线的分歧可精确定义为：**LLM 把考核目标定在出口（翻译回 token 后的样子），JEPA 把考核目标定在半路（直接考核中间的潜向量）**。

### 1.4 目标函数变更的四个层次

LLM 目标函数十年演进（含其前置的表示学习谱系：word2vec、BERT 的静态/掩码表示学习 → GPT 的自回归生成 → T5、FIM → RLHF、DPO → 多 token 预测；其中 word2vec/BERT 属表示学习而非自回归 LLM，列为谱系前置）：损失形式、方向、粒度、辅助信号都在变，但"预测目标锚定在 token 空间"这一不变量保持——它是词表、采样、解码、RLHF 奖励所在的技术栈基座。

变更可分四层：超参数/数据配比（改配置）→ 损失形式（换算法库）→ 预测粒度（改参数）→ **预测目标所在的空间（换数据模型本身）**。JEPA 属于第四层。

---

## 二、现有活跃方向概览

当前多条新路线并行发展，各自攻击不同的技术环节（列举用于定位格局，不代表优劣）：

| 方向 | 核心思想 | 代表工作 |
|------|----------|----------|
| 潜空间预测 | 考核目标从 token 搬到潜向量 | I-JEPA、V-JEPA 2、LLM-JEPA、VL-JEPA |
| 世界模型/生成式模拟器 | 模拟整个可交互的世界 | Genie 3、DreamerV3 |
| 扩散/流匹配语言模型 | 从噪声整体去噪，并行生成 | Mercury、LLaDA |
| 非 Transformer 架构 | 替换注意力机制 | Mamba/SSM、RWKV |
| RL 驱动的推理 | 用奖励信号探索推理过程 | o1/R1 系列（RLVR） |
| 空间智能/3D 原生 | 3D 世界理解 | World Labs |
| 检索增强/外挂记忆 | 知识外置，与推理分离 | RAG 系、MemGPT 类 |
| 发展机器人学 | 按发育时序构建智能（覆盖早期感知运动段） | 各具身智能实验室 |

补充定位（会话中形成的分析框架）：

- **上游与下游**：潜空间表示（世界模型）是中间产物，可直接交付的文本生成是最终产物——两者是上下游关系而非同类竞争；潜空间路线的交付通常需要组合解码器/动作出口。
- **上限与接口**：世界建模类方向抬升感知与规划上限；人类可读交付目前统一经过 token 接口；机器人动作是语言出口之外的交付通道。

---

## 三、参照系：婴儿成长的七条机制

发展心理学与神经科学对婴儿学习机制的研究清单（摇篮的参照清单）：

| # | 机制 | 含义 |
|---|------|------|
| 1 | 具身交互 | 知识从"推、抓、咬、摔"里来，不是从看语料里来 |
| 2 | 自监督预测 | 大脑不停预测下一刻的感觉输入（预测加工理论），猜错了才学 |
| 3 | 多模态对齐 | 摸到的、看到的、听到的，同一事物对应同一种内部表示 |
| 4 | 内在动机 | 好奇心、玩，不需要外部奖励就探索 |
| 5 | 课程学习 | 先学会抓握，再学会因果，再学会语言，难度自动爬坡 |
| 6 | 持续学习 | 5 岁学到的东西 60 岁还在用，边用边改，永不"训练完毕" |
| 7 | 社会学习 | 语言本身就是从交互中学的，不是从下载文本库学的 |

将各方向对照此清单的观察：每条方向各自命中清单中的若干条，**没有一条覆盖全部七条**——因为清单的本质不是七个零件，是七条贯穿的时间线（见 `30-发育图.md`）。

---

## 四、空缺识别

把现有方向映射到"成体图（架构）× 发育图（时序）"两个维度上：

- 架构维度（成体图）：研究密集，工程成熟，是全行业的主战场。
- 时序维度（发育图）：权重继承式多阶段训练、自生成课程、敏感期调度——这些概念在文献中零星散落。发展机器人学是这一维度上最接近的既有方向，但其覆盖止步于早期感知运动段（D1~D2；见 `32-视图对照.md` §1.2），符号及以后的阶段（D3+）未被覆盖——发育过程整体仍未形成独立学科。

**行业缺的不是解剖学，是发育生物学。** 发育路线的制约在交互经历的按序积累——经历必须由系统亲身获得，无法由其它投入形式替代。

---

## 五、收敛：摇篮

由此立项：

- **命名**：行业命名的对象清一色是成体图对象（架构、机制、配方）；"摇篮"命名的是发育图本身——给全行业完成度最低的那张图占位。（注：BAAI 有同名的通用计算机操控 agent 框架 Cradle，方向无关，不冲突。）
- **名字即章程**：摇篮不装大人，只装婴儿——成长判据是发育事件，不是成品能力。
- **谁来摇摇篮**：环境。核心条件是交互时间——摇篮是被环境摇的。
- **立项定位（用户决策）**：作为思想计划落档——只有文档、图、伪代码，不做任何具体实现，只解构整个思想。→ 即 `20-思想文档.md`。
